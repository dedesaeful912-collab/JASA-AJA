const SUPABASE_URL="https://hhmvutunognvlbkeulnf.supabase.co";
const SUPABASE_KEY="sb_publishable_ElleDxRJT85BHFnS8upOmQ_IkvW6MMr";
const supabase=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const $=id=>document.getElementById(id);
let services=[],currentUser=null,currentProfile=null,channel=null;
const STATUS={searching:"Mencari mitra",accepted:"Diterima",on_the_way:"Menuju lokasi",arrived:"Mitra tiba",quoting:"Penawaran harga",awaiting_approval:"Menunggu persetujuan",working:"Sedang dikerjakan",completed:"Selesai",cancelled:"Dibatalkan"};
function esc(v){return String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function money(v){return v==null?"—":"Rp "+Number(v).toLocaleString("id-ID")}
function scrollToId(id){$(id)?.scrollIntoView({behavior:"smooth"})}
function showModal(html){$("modalBody").innerHTML=html;$("modal").classList.remove("hidden")}
function closeModal(){$("modal").classList.add("hidden")}
function msg(type,text){return '<div class="'+type+'">'+esc(text)+'</div>'}
async function getUser(){return (await supabase.auth.getUser()).data.user}
async function loadServices(){const r=await supabase.from("services").select("id,name,description,icon,active").order("name");if(r.error){$("services").innerHTML=msg("error","Gagal memuat layanan");return}services=r.data||[];$("services").innerHTML=services.filter(s=>s.active).map(s=>'<button class="card" data-id="'+s.id+'"><span class="icon">'+esc(s.icon||"🛠️")+'</span><b>'+esc(s.name)+'</b><span>'+esc(s.description||"Pesan mitra profesional")+'</span></button>').join("")||'<div class="loading">Belum ada layanan aktif.</div>';$("service").innerHTML='<option value="">Pilih layanan</option>'+services.filter(s=>s.active).map(s=>'<option value="'+s.id+'">'+esc(s.name)+'</option>').join("");document.querySelectorAll(".card[data-id]").forEach(c=>c.onclick=()=>{$("service").value=c.dataset.id;scrollToId("booking")})}
async function loadProfile(){currentUser=await getUser();currentProfile=null;if(!currentUser)return;const r=await supabase.from("profiles").select("*").eq("id",currentUser.id).single();if(!r.error)currentProfile=r.data}
function authLabel(){$("authBtn").textContent=currentUser?"Keluar":"Masuk / Daftar"}
async function refresh(){await loadProfile();authLabel();if(currentUser){$("dashboard").classList.remove("hidden");await renderDashboard();subscribeRealtime()}else{$("dashboard").classList.add("hidden");if(channel){supabase.removeChannel(channel);channel=null}}}
function subscribeRealtime(){if(channel)supabase.removeChannel(channel);channel=supabase.channel("jasaaja-"+currentUser.id).on("postgres_changes",{event:"*",schema:"public",table:"orders"},()=>renderDashboard()).on("postgres_changes",{event:"*",schema:"public",table:"notifications",filter:"user_id=eq."+currentUser.id},()=>renderDashboard()).subscribe()}
function authForm(mode="login"){showModal('<h2>'+ (mode==="login"?"Masuk":"Daftar") +'</h2><p class="muted">'+(mode==="login"?"Masuk untuk membuat dan mengelola pesanan.":"Buat akun customer atau mitra.")+'</p><label>Nama<input id="fName" placeholder="Nama lengkap" '+(mode==="login"?"style="display:none"":"required")+'></label><label>No. HP<input id="fPhone" placeholder="08xxxxxxxxxx" '+(mode==="login"?"style="display:none":"")+'></label><label>Email<input id="fEmail" type="email" required></label><label>Password<input id="fPass" type="password" minlength="6" required></label>'+(mode==="register"?'<label>Daftar sebagai<select id="fRole"><option value="customer">Customer</option><option value="mitra">Mitra</option></select></label>':"")+'<div class="actions"><button class="cta" id="authSubmit">'+(mode==="login"?"Masuk":"Daftar")+'</button><button class="secondary" id="switchAuth">'+(mode==="login"?"Buat akun":"Sudah punya akun")+'</button></div><div id="authMsg"></div>');$("switchAuth").onclick=()=>authForm(mode==="login"?"register":"login");$("authSubmit").onclick=async()=>{const email=$("fEmail").value.trim(),password=$("fPass").value;let r;if(mode==="login")r=await supabase.auth.signInWithPassword({email,password});else r=await supabase.auth.signUp({email,password,options:{data:{full_name:$("fName").value.trim(),phone:$("fPhone").value.trim(),role:$("fRole").value}}});if(r.error){$("authMsg").innerHTML=msg("error",r.error.message);return}closeModal();await refresh();if(mode==="register"&&!r.data.session)alert("Akun dibuat. Jika konfirmasi email aktif, verifikasi email lalu login.")}}
$("authBtn").onclick=async()=>{if(currentUser){await supabase.auth.signOut();await refresh()}else authForm("login")};
$("locateBtn").onclick=()=>navigator.geolocation?.getCurrentPosition(p=>{$("lat").value=p.coords.latitude;$("lng").value=p.coords.longitude},()=>alert("Izinkan lokasi browser untuk mengambil koordinat."));
$("orderForm").onsubmit=async e=>{e.preventDefault();if(!currentUser){authForm("login");return}if(currentProfile?.role!=="customer"){alert("Akun mitra/admin tidak dapat membuat order customer.");return}const serviceId=$("service").value;if(!serviceId)return alert("Pilih layanan terlebih dahulu.");const payload={customer_id:currentUser.id,service_id:serviceId,address:$("address").value.trim(),problem_description:$("problem").value.trim(),latitude:$("lat").value?Number($("lat").value):null,longitude:$("lng").value?Number($("lng").value):null};const r=await supabase.from("orders").insert(payload).select("id").single();$("result").innerHTML=r.error?msg("error",r.error.message):msg("success","Order dibuat. Mitra online yang sesuai dapat melihatnya.");if(!r.error){e.target.reset();await renderDashboard();scrollToId("dashboard")}};
async function customerOrders(){const r=await supabase.from("orders").select("*,services(name,icon),profiles:profiles!orders_partner_id_fkey(full_name,phone)").eq("customer_id",currentUser.id).order("created_at",{ascending:false}).limit(30);return r.error?[]:r.data||[]}
async function mitraOrders(){const r=await supabase.from("orders").select("*,services(name,icon),profiles:profiles!orders_customer_id_fkey(full_name,phone)").or("partner_id.eq."+currentUser.id+",status.eq.searching").order("created_at",{ascending:false}).limit(30);return r.error?[]:r.data||[]}
function orderCard(o,role){let actions="";if(role==="customer"){if(o.status==="searching")actions='<button class="danger" onclick="cancelOrder(\''+o.id+'\')">Batalkan</button>';if(o.status==="awaiting_approval")actions='<button class="cta" onclick="approveQuote(\''+o.id+'\','+Number(o.offered_price||0)+')">Setujui '+money(o.offered_price)+'</button><button class="danger" onclick="cancelOrder(\''+o.id+'\')">Tolak / Batalkan</button>';if(o.status==="completed")actions='<button class="secondary" onclick="payOrder(\''+o.id+'\','+Number(o.final_price||0)+')">Bayar QRIS / Cash</button><button class="secondary" onclick="reviewOrder(\''+o.id+'\',\''+(o.partner_id||"")+'\')">Beri Rating</button>'}else{if(o.status==="searching")actions='<button class="cta" onclick="acceptOrder(\''+o.id+'\')">Terima Order</button>';if(o.status==="accepted")actions='<button class="cta" onclick="setStatus(\''+o.id+'\',\'on_the_way\')">Menuju lokasi</button>';if(o.status==="on_the_way")actions='<button class="cta" onclick="setStatus(\''+o.id+'\',\'arrived\')">Saya sudah tiba</button>';if(o.status==="arrived")actions='<button class="cta" onclick="quoteOrder(\''+o.id+'\')">Beri penawaran harga</button>';if(o.status==="awaiting_approval")actions='<span class="notice">Menunggu persetujuan '+money(o.offered_price)+'</span>';if(o.status==="working")actions='<button class="cta" onclick="setStatus(\''+o.id+'\',\'completed\')">Tandai selesai</button>'}const nav=o.latitude&&o.longitude?'<a class="secondary" href="https://www.google.com/maps/dir/?api=1&destination='+encodeURIComponent(o.latitude+","+o.longitude)+'" target="_blank" rel="noopener">🗺️ Maps</a>':"";return '<article class="order"><div class="order-head"><div><b>'+esc((o.services?.icon||"🛠️")+" "+(o.services?.name||"Layanan"))+'</b><div class="small">'+esc(o.address)+'</div></div><span class="badge">'+esc(STATUS[o.status]||o.status)+'</span></div><p class="small">'+esc(o.problem_description||"Tanpa catatan")+'</p>'+(o.profiles?'<p class="small">'+esc(role==="mitra"?"Customer: ":"Mitra: ")+esc(o.profiles.full_name||"")+(o.profiles.phone?" · "+esc(o.profiles.phone):"")+'</p>':"")+(o.offered_price?'<p><b>Penawaran: '+money(o.offered_price)+'</b></p>':"")+(o.final_price?'<p><b>Harga akhir: '+money(o.final_price)+'</b></p>':"")+'<div class="actions">'+actions+nav+'</div></article>'}
async function renderDashboard(){if(!currentUser)return;if(currentProfile?.role==="customer")return renderCustomer();if(currentProfile?.role==="mitra")return renderMitra();if(currentProfile?.role==="admin")return renderAdmin()}
async function renderCustomer(){const orders=await customerOrders();$("dash").innerHTML='<div class="heading"><span class="tag">CUSTOMER</span><h2>Pesanan Saya</h2><p class="muted">Halo, '+esc(currentProfile.full_name||currentUser.email)+'</p></div><div class="toolbar"><button class="secondary" onclick="scrollToId(\'booking\')">+ Pesan jasa</button><button class="secondary" onclick="renderDashboard()">↻ Refresh</button></div>'+(orders.length?orders.map(o=>orderCard(o,"customer")).join(""):'<div class="loading">Belum ada pesanan.</div>')}
async function renderMitra(){const mine=await mitraOrders();const pp=await supabase.from("partner_profiles").select("*").eq("id",currentUser.id).maybeSingle();const ps=await supabase.from("partner_services").select("service_id").eq("partner_id",currentUser.id);const selected=(ps.data||[]).map(x=>x.service_id);$("dash").innerHTML='<div class="heading"><span class="tag">MITRA</span><h2>Dashboard Mitra</h2><p class="muted">Verifikasi: '+(pp.data?.verified?"Terverifikasi":"Menunggu admin")+'</p></div><div class="grid2"><div class="stat"><b>'+(pp.data?.online?"ONLINE":"OFFLINE")+'</b><div class="small">'+(pp.data?.online?"Menerima":"Tidak menerima")+' order.</div><button class="secondary" onclick="toggleOnline('+(pp.data?.online?"false":"true")+')">'+(pp.data?.online?"Offline":"Online")+'</button></div><div class="stat"><b>'+mine.filter(x=>x.partner_id===currentUser.id).length+'</b><div class="small">Order Anda</div></div></div><div class="order"><b>Layanan</b><div class="grid2">'+services.map(s=>'<label><input type="checkbox" '+(selected.includes(s.id)?"checked":"")+' onchange="toggleService(\''+s.id+'\',this.checked)"> '+esc(s.icon||"🛠️")+' '+esc(s.name)+'</label>').join("")+'</div></div><h3>Order tersedia / aktif</h3>'+(mine.length?mine.map(o=>orderCard(o,"mitra")).join(""):'<div class="loading">Belum ada order.</div>')}
async function renderAdmin(){const [u,o,p,r,s]=await Promise.all([supabase.from("profiles").select("id,full_name,phone,role,created_at").order("created_at",{ascending:false}).limit(100),supabase.from("orders").select("id,status,customer_id,partner_id,service_id,final_price,created_at,services(name)").order("created_at",{ascending:false}).limit(100),supabase.from("partner_profiles").select("id,verified,online,address").limit(100),supabase.from("reviews").select("rating").limit(100),supabase.from("services").select("*").order("name")]);const users=u.data||[],partners=p.data||[],orders=o.data||[];$("dash").innerHTML='<div class="heading"><span class="tag">SUPER ADMIN</span><h2>Control Center</h2></div><div class="grid2"><div class="stat"><b>'+users.length+'</b><div class="small">Pengguna</div></div><div class="stat"><b>'+partners.length+'</b><div class="small">Mitra</div></div><div class="stat"><b>'+orders.length+'</b><div class="small">Order</div></div><div class="stat"><b>'+((r.data||[]).length)+'</b><div class="small">Review</div></div></div><h3>Mitra</h3><table class="table"><tr><th>Nama</th><th>Status</th><th>Aksi</th></tr>'+users.filter(x=>x.role==="mitra").map(x=>{const pp=partners.find(p=>p.id===x.id);return '<tr><td>'+esc(x.full_name||x.phone||x.id.slice(0,8))+'</td><td>'+(pp?.verified?"Verified":"Belum")+'</td><td>'+(pp?'<button class="secondary" onclick="verifyPartner(\''+x.id+'\','+(!pp.verified)+')">'+(pp.verified?"Cabut":"Verifikasi")+'</button>':"—")+'</td></tr>'}).join("")+'</table><h3>Layanan</h3><div class="actions"><button class="cta" onclick="addService()">+ Tambah layanan</button></div><table class="table"><tr><th>Layanan</th><th>Aktif</th><th>Aksi</th></tr>'+(s.data||[]).map(x=>'<tr><td>'+esc(x.icon||"")+" "+esc(x.name)+'</td><td>'+(x.active?"Ya":"Tidak")+'</td><td><button class="secondary" onclick="toggleActive(\''+x.id+'\','+(!x.active)+')">'+(x.active?"Nonaktifkan":"Aktifkan")+'</button></td></tr>').join("")+'</table>'}
async function acceptOrder(id){const r=await supabase.from("orders").update({partner_id:currentUser.id,status:"accepted"}).eq("id",id).eq("status","searching");if(r.error)alert(r.error.message);await renderDashboard()}
async function setStatus(id,status){const r=await supabase.from("orders").update({status}).eq("id",id);if(r.error)alert(r.error.message);await renderDashboard()}
async function quoteOrder(id){showModal('<h2>Penawaran Harga</h2><label>Harga<input id="quote" type="number" min="1"></label><button class="cta" id="sendQuote">Kirim penawaran</button>');$("sendQuote").onclick=async()=>{const price=Number($("quote").value);if(!price)return;const r=await supabase.from("orders").update({offered_price:price,status:"awaiting_approval"}).eq("id",id);if(r.error)alert(r.error.message);closeModal();await renderDashboard()}}
async function approveQuote(id,price){const r=await supabase.from("orders").update({final_price:price,status:"working"}).eq("id",id).eq("status","awaiting_approval");if(r.error)alert(r.error.message);await renderDashboard()}
async function cancelOrder(id){if(!confirm("Batalkan pesanan?"))return;const r=await supabase.from("orders").update({status:"cancelled"}).eq("id",id);if(r.error)alert(r.error.message);await renderDashboard()}
async function toggleOnline(value){if(!currentProfile?.role||currentProfile.role!=="mitra")return;const r=await supabase.from("partner_profiles").upsert({id:currentUser.id,online:value==="true",updated_at:new Date().toISOString()});if(r.error)alert(r.error.message);await renderDashboard()}
async function toggleService(serviceId,on){const r=on?await supabase.from("partner_services").upsert({partner_id:currentUser.id,service_id:serviceId}):await supabase.from("partner_services").delete().eq("partner_id",currentUser.id).eq("service_id",serviceId);if(r.error)alert(r.error.message)}
async function payOrder(orderId,amount){showModal('<h2>Pembayaran</h2><p>Total: <b>'+money(amount)+'</b></p><label>Metode<select id="payMethod"><option value="qris">QRIS</option><option value="cash">Cash</option></select></label><div class="notice">QRIS gateway belum dikonfigurasi. Catatan pembayaran dibuat pending sampai dikonfirmasi.</div><button class="cta" id="payBtn">Catat pembayaran</button>');$("payBtn").onclick=async()=>{const r=await supabase.from("payments").upsert({order_id:orderId,method:$("payMethod").value,amount,status:"pending"});if(r.error)alert(r.error.message);else alert("Pembayaran tercatat sebagai pending.");closeModal();await renderDashboard()}}
async function reviewOrder(orderId,partnerId){showModal('<h2>Beri Rating</h2><div class="stars">'+[1,2,3,4,5].map(n=>'<button onclick="submitReview(\''+orderId+'\',\''+partnerId+'\','+n+')">★</button>').join("")+'</div><textarea id="reviewText" placeholder="Ulasan (opsional)"></textarea>')}
async function submitReview(orderId,partnerId,rating){const r=await supabase.from("reviews").insert({order_id:orderId,customer_id:currentUser.id,partner_id:partnerId,rating,review:$("reviewText").value.trim()});if(r.error)alert(r.error.message);else alert("Rating tersimpan.");closeModal()}
async function verifyPartner(id,verified){const r=await supabase.from("partner_profiles").upsert({id,verified,updated_at:new Date().toISOString()});if(r.error)alert(r.error.message);await renderDashboard()}
async function toggleActive(id,active){const r=await supabase.from("services").update({active}).eq("id",id);if(r.error)alert(r.error.message);await loadServices();await renderDashboard()}
async function addService(){showModal('<h2>Tambah Layanan</h2><label>Nama<input id="sn"></label><label>Icon<input id="si" placeholder="🛠️"></label><label>Deskripsi<textarea id="sd"></textarea></label><button class="cta" id="addBtn">Simpan</button>');$("addBtn").onclick=async()=>{const r=await supabase.from("services").insert({name:$("sn").value.trim(),icon:$("si").value.trim()||"🛠️",description:$("sd").value.trim(),active:true});if(r.error)alert(r.error.message);closeModal();await loadServices();await renderDashboard()}}
async function init(){await loadServices();await refresh();supabase.auth.onAuthStateChange(()=>setTimeout(refresh,0))}
init();